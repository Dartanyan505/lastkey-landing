<section id="destek" class="section section--tight section--cta">
  <div class="container">
    <div class="final-cta">
      <div>
        <h2>Destek Formu</h2>
        <p class="muted">Sorunu veya talebini bize yaz. Ekibimiz en kısa sürede seninle iletişime geçsin.</p>
      </div>

      <form class="support-form" action="#" method="post">
        <label class="field">
          <span>Ad Soyad</span>
          <input type="text" name="name" placeholder="Ad Soyad" required />
        </label>

        <label class="field">
          <span>E-posta</span>
          <input type="email" name="email" placeholder="ornek@mail.com" required />
        </label>

        <label class="field field--full">
          <span>Mesaj</span>
          <textarea name="message" rows="4" placeholder="Destek talebini kısaca yaz." required></textarea>
        </label>

        <div class="support-form__actions">
          <button class="btn btn--lg" type="submit">Gönder</button>
        </div>
      </form>
    </div>

    <div class="support-to-top-wrap">
      <button class="support-to-top" type="button" data-to-top aria-label="Sayfanın başına dön">
        <svg class="support-to-top__icon" viewBox="0 0 24 24" aria-hidden="true" focusable="false">
          <path d="M6.5 14.5 12 9l5.5 5.5" />
        </svg>
      </button>
    </div>
  </div>
</section>

<script>
  const supportToTopButtons = document.querySelectorAll("[data-to-top]");
  supportToTopButtons.forEach((button) => {
    button.addEventListener("click", () => {
      window.scrollTo({ top: 0, behavior: "smooth" });
    });
  });
</script>
